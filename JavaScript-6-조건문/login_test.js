var id = prompt("Type your ID : ");
      var password = prompt("Type your Password : ");
      if (id === "myeonghyun" && password == 1111) {
        alert("You're now safely logged in!"); 
      } else {
        alert("Login failed!");